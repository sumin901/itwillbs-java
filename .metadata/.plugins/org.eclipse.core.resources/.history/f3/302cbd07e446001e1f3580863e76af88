package constructor_overloading;

public class Test {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}


/*
 * 날짜를 관리하는 MyDate 클래스 정의
 * 
 *  - 멤버변수 : 연도(year, int), 월(month, int), 일(day, int)
 *  
 *  - 생성자 
 *   1) 기본 : 1999년 1월 1일로 초기화
 *   2) 파라미터 생성자 : 
 *    2-1) 연도를 전달받아 초기화하고, 1월 1일로 초기화
 *    2-2) 연도, 월을 전달받아 초기화하고, 1일로 초기화
 *    2-3) 연도, 월, 일을 전달받아 초기화 
 */

class MyDate {
	
	int year;
	int month;
	int day;
	
	public MyDate(int year, int month, int day) {
		System.out.println("Mydate(int, int, int) 호출");
		this.year = year;
		this.month = month;
		this.day = day;
	}

	public MyDate(int year, int month) {
		System.out.println("Mydate(int, int) 호출");
		this.year = year;
		this.month = month;
	}

	public MyDate(int year) {
		
		this.year = year;
	}
	
	
	
	
	
	
}